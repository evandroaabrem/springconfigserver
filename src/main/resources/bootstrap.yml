spring:
  application:
    name: config-server

  cloud:
    aws:
      region:
        static: us-east-2

    secretsmanager:
      enabled: true

    config:
      server:
        awssm:
          enabled: true